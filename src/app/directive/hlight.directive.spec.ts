import { HlightDirective } from './hlight.directive';

describe('HlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HlightDirective();
    expect(directive).toBeTruthy();
  });
});
